<!-- res/drawable/circular_checkbox_background.xml -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_checked="true">
        <layer-list>
            <item>
                <shape android:shape="oval">
                    <solid android:color="#FFFFFF" /> <!-- Background color when checked -->
                    <stroke android:width="2dp" android:color="@color/primaryColor" /> <!-- Border color when checked -->
                    <size android:width="20dp" android:height="20dp" /> <!-- Size -->
                </shape>
            </item>
            <item android:drawable="@drawable/check_circle" /> <!-- Checkmark drawable overlay -->
        </layer-list>
    </item>
    <item android:state_checked="false">
        <shape android:shape="oval">
            <solid android:color="#FFFFFF" /> <!-- Background color when unchecked -->
            <stroke android:width="1dp" android:color="@color/primaryColor" /> <!-- Border color when unchecked -->
            <size android:width="20dp" android:height="20dp" /> <!-- Size -->
        </shape>
    </item>
</selector>
